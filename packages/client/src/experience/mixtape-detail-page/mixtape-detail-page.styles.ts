import styled from "@emotion/styled";

import { rhythm } from "../../fasteners/styles";

export const Frame = styled.div`
  width: 100%;
  max-width: 490px;
  margin: 0 auto;
  padding-top: ${rhythm(5)};
  padding-bottom: ${rhythm(5)};
`;
